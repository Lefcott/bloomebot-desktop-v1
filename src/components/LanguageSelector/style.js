import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  inputLabel: {
    textAlign: 'center',
    margin: 10
  },
  formControl: {},
  select: {
    margin: 8,
    padding: 2,
    textAlign: 'center'
  }
});
